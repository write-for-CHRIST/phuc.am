{
  "name": "phucam",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build:clean": "rimraf build",
    "build:deploy": "node run ./tools/gh-pages-publish.js",
    "release": "run-s build:clean build build:deploy"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "travis-deploy-once": "^5.0.9"
  },
  "dependencies": {
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "1.1.5"
  }
}
